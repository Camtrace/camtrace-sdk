{
  "name": "camtrace-demo",
  "version": "1.2.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "apps/demo",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace packages/api --workspace packages/decoder",
    "demo": "npm run dev --workspace apps/demo"
  }
}
